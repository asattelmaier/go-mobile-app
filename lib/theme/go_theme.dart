import 'package:flutter/material.dart';

class GoTheme {
  Color primaryLight = Color.fromRGBO(242, 242, 240, 1.0);

  Color primaryDark = Color.fromRGBO(76, 79, 89, 1);

  Color secondaryLight = Color.fromRGBO(217, 145, 82, 1);

  Color secondaryDark = Color.fromRGBO(140, 61, 43, 1);

  Color accent = Color.fromRGBO(107, 191, 23, 1);

  double borderWidth = 2;

  double gutter = 2;

  GoTheme();
}
